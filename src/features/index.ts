// Copyright 2021-2024 Atlas
// Author: Atlas (atlas@vialabs.io)

import { IFeature } from "../types/IFeature.js";

/**
 * Array of available features in the system.
 * Add new features to this array to make them available for processing.
 */
const features: IFeature[] = [];

export default features;
